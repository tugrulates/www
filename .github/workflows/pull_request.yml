name: Pull Request

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  preview:
    uses: ./.github/workflows/ci.yml
    secrets: inherit
    with:
      environment: preview

  comment:
    needs: preview
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/download-artifact@v3
        with:
          name: reports
          path: reports
      - name: Deploy
        id: deploy
        run: echo json=$(npm run -s deploy -- --message reports --dir=reports --json) >> ${GITHUB_OUTPUT}
        env:
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      - uses: exercism/pr-commenter-action@v1.3.0
        with:
          github-token: "${{ github.token }}"
          template-variables: |
            {
              "id":        "${{ fromJSON(steps.deploy.outputs.json).deploy_id }}",
              "reports":   "${{ fromJSON(steps.deploy.outputs.json).deploy_url }}",
              "result":    "${{ needs.preview.result }}",
              "preview":   "${{ needs.preview.outputs.url }}",
              "lighthouse": ${{ needs.preview.outputs.lighthouse }}
            }
