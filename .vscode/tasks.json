{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Jekyll: Run Server",
            "type": "shell",
            "command": "bundle exec jekyll serve --unpublished --future --config _config.yml --livereload --force-polling",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
