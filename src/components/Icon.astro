---
import { Image } from "astro:assets";

const { src, alt, className, size } = Astro.props;
---

<Image
  src={src}
  alt={alt}
  width={size ?? 32}
  height={size ?? 32}
  class:list={["brightness-0 dark:brightness-100", className]}
/>
